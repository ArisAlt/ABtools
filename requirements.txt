mutagen
requests
beautifulsoup4
rapidfuzz
rich
tqdm
# Renamed from duckduckgo-search; the old name still works but warns on
# every search. ablib.providers.mcp falls back to it if ddgs is absent.
ddgs
# mcp_server/server.py uses the v1 FastMCP API. mcp 2.x renamed FastMCP to
# MCPServer, so pin <2 until mcp_server is migrated.
mcp<2
